// Keep a single Firebase initializer for the whole client application.
// This compatibility module avoids creating a second default app if it is used later.
export { app } from '../config/firebase';
